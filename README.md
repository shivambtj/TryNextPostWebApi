# TryNextPostWebApi
using asp.net core web api+ sql server where login(jwt auth,register will send mail to register user) and role master also completed
1. register->send mail to register mail id
   login->password hashing(encrypeted), jwt token generated
   forgetpassword-> send otp,verify otp ,create new password and login
2. mail setting for dynamic mail sender desided by admin/super admin
3. role master for create role like admin,super admin,seller every register user by default seller.
